import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  btn: {paddingBottom: '10%'},
  container: {justifyContent: 'center', alignItems: 'center', flex: 1},
  imgst: {width: 200, height: 140},
});

export default styles;
